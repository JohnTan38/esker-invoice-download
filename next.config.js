/**
 * @type {import('next').NextConfig}
 *
 * Minimal Next.js configuration. React strict mode is enabled to surface
 * potential problems in development. Additional configuration (such as
 * internationalisation or image domains) can be added here if required.
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;